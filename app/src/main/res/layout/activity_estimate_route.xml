<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EstimateRoutesActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:background="@color/colorPrimary"
        android:elevation="5dp">

    </LinearLayout>
    <android.support.design.widget.TabLayout
        android:id="@+id/EstimateRoutesTabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="5dp"
        android:background="@color/colorPrimary"
        app:tabIndicatorHeight="0dp"
        app:tabTextColor="@color/maybeGray"
        app:tabSelectedTextColor="@color/defaultGray"
        app:tabTextAppearance="@style/DefaultTabLayout"
        />

    <android.support.v4.view.ViewPager
        android:id="@+id/EstimateRoutesPager"
        android:layout_below="@id/EstimateRoutesTabs"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


</LinearLayout>